@page
@model Template.Pages.SearchModel
<h2>Search Entities</h2>

<form method="post">
    <p>
        <label>Animal:</label>
        <select asp-for="AnimalIdSelection" asp-items="@(Model.AnimauxList)">
            <option value="">-- Select Animal --</option>
        </select>
    </p>
    <p>
        <label>Fruit:</label>
        <select asp-for="FruitIdSelection" asp-items="@(Model.FruitsList)">
            <option value="">-- Select Fruit --</option>
        </select>
    </p>
    <button type="submit">Search</button>
</form>
